---
interface Props {
  href?: string;
  class?: string;
}
const { href = '/products/hardware', class: className = '' } = Astro.props;
---

<a class={`btn btn-primary${className ? ` ${className}` : ''}`} href={href} aria-disabled="false">
  Coming soon
</a>
